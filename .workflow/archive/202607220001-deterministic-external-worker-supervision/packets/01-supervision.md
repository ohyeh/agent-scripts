# Packet 01 — supervision

Implement the approved mandatory native proxy, deterministic blocking wrapper,
ownership transfer, quiet unchanged waiting, and artifact-only routine gate
receipts across `agent-scripts` and `tmux-agent-tools`.
