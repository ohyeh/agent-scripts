# Orchestration

GATE: ~/.agents/rules/maintenance.md §1 — "Global files (\`~/.claude/CLAUDE.md\`, \`~/.codex/AGENTS.md\`) | Nothing | Everything (edit BOTH in the same change; \`Version:\` lines must stay identical)" | this task: user requested clarification, but canonical edits wait for the exact wording below to be approved.

Approval: user reviewed and approved the exact diff. The optional `using-workflows`
qualification was declined because approved designs may use a different domain router.

1. Present exact wording.
2. On approval, apply it to both global files and the canonical adapter reference.
3. Verify, commit, push, deploy the pushed branch, then verify deployed parity.
