# Model dispatch efficiency

Goal: reduce repeated gate noise, prevent model-intent fabrication, and design a live-verified Codex model/effort policy.

Success criteria:
- Approved gate and authorization rules are applied identically to both global files.
- Codex model and effort capabilities are verified from the live runtime before a second semantic diff is proposed.
- Repo checks pass; publish to `main` before deploying the downloaded canonical tree locally.

Plan:
1. Apply the user-approved rules diff.
2. Inventory Codex CLI, native sub-agent, and wrapper model/effort surfaces.
3. Propose the smallest efficiency matrix as an exact diff for approval.
4. Commit and publish to `main`, then deploy and verify the local runtime copies.
