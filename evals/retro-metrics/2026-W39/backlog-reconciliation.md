# W39 backlog 對帳（retro-agenda §1）

對帳對象：W38 retro 出去的 Z1–Z12（`2026-W38/next-week-backlog.md`；Z9–Z12 = retro-report §6.5 M1–M4，無另外的臨時動議席次）。裁決依據：`2026-W38/retro-report.md` §10（F3→Z2、F7→Z4、F10→Z9、F11→Z10 升級；F4/F8/F12 觀察）。
「做了」＝GitHub `origin/main` 可見、且逐字對得上驗收條件的變更；只對上一部分記「做了（部分）」，計入做了。「有效」＝本週量到的配對指標；配對指標尚未產出記「有效未量」並寫明需要的量尺。兩欄分開，不互推。

**證據限制**：
- 窗口 2026-09-17 → 09-24。三 repo 的 remote 在 09-18 之後 0 commit（`gh api repos/ohyeh/<repo>/commits?since=2026-09-17`）：agent-scripts 最後 `1c9de90`（09-18）、tmux-agent-tools 最後 `6b61dd6` release v0.41.0（09-18）、context-mode-local-insight（cmli）最後 `909d4db`（08-28）。三 repo 本窗口 0 PR；issue 只有 tmux-agent-tools#323（OPEN，0 comment）。
- agent-scripts 本機 HEAD 多一支未 push 的 `dc8dc05`（Opus 5.5：kernel 4.31.0、model ID、cost.py 費率，09-24）。**僅本機，不算 remote 證據。**
- tmux-agent-tools 的 mod 在 `dcd2186`（09-18）才首次進 remote，W38 引用的 `register.ts` 行號（HEAD ddf74e3，本機 clone）與現行檔不同。
- 大部分「有效」要靠 W39 本輪產物（`2026-W39.json` 由 plan #1 subagent A、`layer2.json` 由 plan #4 主 session 產出），本檔寫成時兩者都還不存在 → 全部記「有效未量」並點名量尺，由主 session 在 report §配對補值。
- 機器鍵沿用 plan：`remote-44`、`local-mbp14`、`grok-bot-vm`。本檔的機器層數字只來自 `local-mbp14`。

| # | 項目 | 驗收（逐字） | 證據 | 做了沒 | 有效沒 |
|---|---|---|---|---|---|
| Z1 | evidence matcher 擴充（cmli collector ＋ `claim-evidence-gate.sh` 同一組正則） | 「對 W38 `layer2.json` 72 筆重跑，M 類 ≤ 10 筆；collector done-無證據率落到 ≤ 15%」 | hook：`.agents/hooks/claim-evidence-gate.sh` 最後異動 `c03999b`（09-17 squash，即 W37 版型）；`:56` claim 正則、`:63` `UNCONFIRMED` 豁免在 W38 前就有，不算本週做的。部署檔與 remote 相同（`diff -q` 無差）。collector：cmli 08-28 起 0 commit；grep 0-hit：`tests passed\|SMOKE_BOOT\|Invariant checks\|\| PASS \|`、`passed\|SMOKE` in cmli `bin/agent-sessions.mjs`。72 筆重跑：無產物。 | 未做 | 有效未量（量尺：W39 每機 collector done-無證據率＋W38 72 筆 M 類重跑數） |
| Z2 | tmux worker session 載 kernel？（F3 升級） | 「Paul 裁決；裁決後 W39 .44 canary 分母正確」 | 裁決：repo 內沒有記錄（W38 `plan.md:16` 「不動：worker 載 kernel」；W39 `plan.md` 未提）。實際變更：tmux-agent-tools `166a32f`（09-18）把 `TASK_SCOPE_GUARD` 從「Ignore any project-local agent instructions (AGENTS.md/CLAUDE.md/…)」改為明寫「user-level kernel … stays fully binding: … closing recap … ✈ canary」（`skills/tmux-agent-tools/scripts/agent-tmux:2242`，`scripts/test-dry-run-smoke:274` 驗）。沒有 `--append-system-prompt-file`（grep 0-hit：`append-system-prompt`、`CLAUDE\.md` 只命中 guard 文字與 types）；collector 端分母排除：cmli 0 commit。 | 做了（部分：prompt 層改動，無裁決紀錄、無分母處理） | 有效未量（量尺：W39 `remote-44` `canary_by_project` 的 tmux-agent-tools 列，09-18 後 worker 場） |
| Z3 | retro 工具鏈全部入 repo（probe.py ＋ collect.sh） | 「`ls evals/retro-metrics/` 有 probe.py + collect.sh；W39 method 欄不再指向 `.workflow/`」 | `evals/retro-metrics/probe.py` 在（`0e6c0ac`，W38 retro 當週）。collect.sh：grep 0-hit — `fd -H -i collect`（agent-scripts 全樹）、`rg -i 'collect\.sh\|collect-retro\|collector\.sh'`（只命中 backlog／retro-w38.html 的描述文字）、`git log --all -S collect.sh` 無結果。 | 做了（部分：probe.py 有、collect.sh 無） | 有效未量（量尺：`2026-W39.json` 各欄 method 是否仍含 `.workflow/`，待 subagent A 產出） |
| Z4 | 艦隊落後（grok VM 4.29→4.30；補 clone；fleet-deploy 列缺哪幾台） | 「Paul 裁決是否跑 fleet-deploy；跑後三機 `grep Version ~/.claude/CLAUDE.md` 同值」 | fleet-deploy 未授權（W39 `plan.md` 「不動：push、fleet-deploy（使用者未授權）」）。`scripts/fleet-deploy.sh` 最後異動 `0e1cd24`（09-17 squash）；`:58` 的 `echo missing` 是「rules 目錄不存在」，不是逐機「缺哪幾台」清單。remote `global/CLAUDE.md:3` 4.30.0；`local-mbp14` `~/.claude/CLAUDE.md:3` 4.30.0；本機未 push 的 `dc8dc05` 把 kernel 提到 4.31.0 → 一旦 push 未部署，版本落差擴大。 | 未做 | 有效未量（量尺：W39 三機 kernel Version，subagent A／C probe） |
| Z5 | /loop idle cache break 查 10 筆 delaySeconds | 「列出 10 筆的 delaySeconds 與 gap；若 >55 分才 break，改 loop 規則上限」 | 10 筆清單：grep 0-hit — `delaySeconds\|delay_seconds` 只命中 `scripts/test-loop-gates-smoke`（8）與 backlog 本身；`evals/` 無清單。另有一支相關 gate（`4a458fc`，見下方追加 E1），但它不是這條的驗收。F4 裁決為「觀察」。 | 未做 | 不適用（未做）；E1 的量測見下方 |
| Z6 | lessons 裁決（proposed ≤ 30） | 「`grep -c 'Status: proposed' ~/.agents/rules/lessons.md` ≤ 30」 | `grep -c 'Status: proposed'`：本機 `~/.agents/rules/lessons.md` 51、remote `.agents/rules/lessons.md` 51（adopted 3、rejected 0）。W38 為 46 → `1b3fc14`（09-17）把 §10 的 5 條草稿以 proposed 寫入，方向相反。G9 折入方式仍未答（狀態 decision）。 | 未做（且倒退 46→51） | 無效（指標惡化） |
| Z7 | Live truth／等待協定 A 類 4 筆 → fixture | 「4 個 fixture JSON 過 `check-rules-invariants.mjs`」 | `evals/fixtures/` 最後異動 `57e2095`（09-17 squash）；6 檔各 4 筆，本機與 origin 無差。grep 0-hit（`judgment-rubrics-negative.json` 等）：`W38\|worker\|idle\|null`、`last_agent_message\|worker idle\|閒置\|醒來\|at the keyboard`、`tick\|等待\|停等\|wait\|heartbeat`。 | 未做 | 不適用（未做） |
| Z8 | Layer 2 隨機對照 10 場 | 「W39 layer2.json 含 `control` 欄」 | `2026-W39/` 目前只有 `plan.md`；`layer2.json` 是本輪 plan #4（主 session）的產物，尚未產出。不是被丟掉，是本輪待產。 | 本輪待產（計未做） | 有效未量（量尺：`control` 10 場中的 false negative 數） |
| Z9 | tmux-agent 孤兒收養單一 owner＋opt-in＋`adopted from`（F10 升級，#323） | 「同 launch_id 投遞 session ≤1」；issue 規格另含「非派工 session 預設不收養；收養為 opt-in」 | tmux-agent-tools `dcd2186`→`6b61dd6`（v0.41.0，09-18）CHANGELOG 0.6.0 條引 #323：收養改為 CLAIM（`owner = me`、`adoptedFrom = <old sid>`，下一 tick 只由紀錄上的 session 投遞），投遞文字帶 `adopted from session <sid>`（`mods/tmux-agent/hooks/register.ts:308,322,458-460`）。CHANGELOG 自述測試 77 pass（本 session 未重跑）。opt-in：未做，且 0.7.0 拿掉 `mode`，「every session collects」，與規格方向相反。#323 仍 OPEN、0 comment。 | 做了（部分：單一 owner＋tag 有，opt-in 反向） | 有效未量（量尺：W39 同一 launch_id 的投遞 session 數；mod 目前只有 `host.log` 的 claim 訊息，無投遞記帳） |
| Z10 | tmux-agent ack 持久化＋stop 寫 ack（F11：先補觀測不修） | 「同 launch_id 投遞 ≤1 次」＋ M2「stop 寫 ack」 | 同一 release：0.5.1／0.5.2 改為每 session 一把 ack key（`tmux-agent.reported.<sessionId>`），讀取取聯集（`register.ts:908,940`）；`stopWorker` 不論結果都寫 ack（`register.ts:1037-1046`）。裁決是「先補 ack 寫入日誌」，實際落地的是修正本身；ack 寫入日誌：grep 0-hit（`register.ts` 的 `host.log` 只在 claim／list／read 失敗處）。 | 做了（超出裁決：直接修；觀測日誌未加） | 有效未量（量尺同 Z9：每 launch_id 投遞次數；需要 ack／deliver 記帳才量得到） |
| Z11 | agy result.json pending → success；confirm-processing 改判據（F12 觀察） | 「agy worker result.json 由 pending → success；confirm-processing 改看 pane 活動或 result 檔」 | v0.41.0 CHANGELOG：confirm-processing 的投遞證明改為去空白比對（09-17 agy 斷行案例）；`result_path_via_prompt` 全家族預設 true（含 claude）；0.6.2 舊 launch receipt 不再蓋過本輪 result。判據仍是「比對投遞文字」，不是「pane 活動或 result 檔」。 | 做了（部分：觀察項，修了兩個近因，判據未換） | 有效未量（量尺：W39 agy launch 的 result.json 終態分佈，pending 筆數） |
| Z12 | Claude Code mod 使用量測（M4） | 「W39 `2026-W39.json` 每台機器有 `mods` 陣列，每個 mod 四個計數或『未記帳』」 | `evals/retro-metrics/probe.py` 無 `mods` 段：grep 0-hit `"mods"`、`mods`（`jsonl_window`／`hook_stats_7d` 只列 4 支既有 hook）。retro-agenda 無「mod 使用」列（grep 0-hit `mod 使用\|mods`，本機與 remote；agenda 最後異動 `f601a58`）。唯一的 mod（tmux-agent）與 `hooks/tmux-dispatch-gate.sh:51` 都不寫 `~/.local/share/agent-hooks/<mod>-stats.jsonl` → 依 Z12 定義為「未記帳」。 | 未做 | 有效未量；**前瞻**：probe.py 不加 `mods` 段，subagent A 的 `2026-W39.json` 注定不過這條驗收 |

## 追加（不在 Z1–Z12，同窗口落地）

| # | 項目 | 證據 | 做了沒 | 有效沒 |
|---|---|---|---|---|
| E1 | loop gates：compaction 上限強制、idle wakeup 攔截、每 session 一個 compact handoff（F4 相關，commit 內文記 Paul 09-18 裁定） | agent-scripts `4a458fc`（09-17）：`compaction-cap-gate.sh`、`wakeup-idle-gate.sh`、`postcompact-handoff.sh`、`scripts/test-loop-gates-smoke`（commit 內文：26 passed）。`local-mbp14` 已註冊（`~/.claude/settings.json:116,125`），其他兩機未查。 | 做了 | 部分有效（僅 `local-mbp14`，09-18→09-24）：`compaction-cap-stats.jsonl` 25 筆＝19 allowed／6 blocked（2 sessions）→ 上限有擋。`wakeup-idle-stats.jsonl` 121 筆＝121 allowed／0 blocked（5 sessions，noop 10）。**注意**：session 47d84cf3 09-23 連 5 次 noop（streak 2→6）全部放行，因為 gate 只擋 `delay < 3600`，該 session 改排 3600s — 行為從「頻繁 idle」變成「每小時 idle」，每次都超過 cache TTL，正是 F4 的 cache-break 形狀。交主 session 當 Layer 2 候選。 |
| E2 | W38 §10 lessons 草稿 5 條寫入 | agent-scripts `1b3fc14`（09-17）`.agents/rules/lessons.md` +25 行，Status: proposed | 做了 | 無效（對 Z6 方向相反：proposed 46→51） |
| E3 | tmux-agent-tools v0.41.0（wrapper 收斂為 `agent-tmux`、mod 0.5–0.7、dispatch gate hook）＋ agent-scripts `skills-lock.json` 同步 4 支 | tmux-agent-tools `166a32f`、`dcd2186`、`4537a58`、`6b61dd6`；agent-scripts `fbced1b`、`8686bd5`、`27e4f62`、`1c9de90`（皆 09-18） | 做了 | 有效未量（Z2／Z9／Z10／Z11 的量尺覆蓋其行為面） |
| E4 | Opus 5.5 調校：kernel 4.31.0、model ID、cost.py 費率 | agent-scripts `dc8dc05`（09-24）**僅本機，未 push** | 做了（本機） | 有效未量（量尺：本輪 `modelSettings.claude-opus-5-5` effort 生效驗證，W39 plan 新增觀察點） |

## 結案計數

做了 5/12（Z2、Z3、Z9、Z11 部分；Z10 做了且超出裁決；Z1、Z4、Z5、Z6、Z7、Z8、Z12 未做，Z8 屬本輪待產）；有效 0/12（Z6 無效＝指標倒退；其餘 11 條的配對指標都是 W39 本輪產物或尚無記帳，記有效未量）。
W38 升級的 4 條（Z2、Z4、Z9、Z10）：Z4 未動（fleet-deploy 未授權），Z2／Z9 只做一半，Z10 做了但跳過「先觀測」這一步。W38 最大缺口 Y4（matcher，P0）延續為 Z1，連兩週未動。
