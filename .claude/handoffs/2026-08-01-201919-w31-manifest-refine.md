# Handoff: W31 砍伐季收尾 — manifest 精煉、四份收斂為兩份

## Session Metadata
- Created: 2026-08-01 20:19:19
- Project: /Users/paul.yeh/github/agent-scripts
- Branch: main
- Session duration: 全日（W31 砍伐季主 session，經多次 compact）

## Recent Commits (for context)
  - 242803a style(workflow-manifest): 去贅述 — 同一收斂事實只講一次，doctrine 細節只留 §04
  - fddfa2a fix(workflow-manifest): 救回 00 L1 單題閉環 SVG（54aeec1 重寫時誤刪）
  - 2e883f9 refactor(manifests): 精煉 — 刪除已完結 spinout-work-order、workflow-manifest 補記併入正文、skill-manifest footer 加 lock hash 戳記
  - e9e1025 docs(manifests): W31 addendum — consensus-gate sole second-model entry; spinout W7 evidence updated
  - bc16e52 chore(skills): re-source 11 vendored skills to ohyeh in canonical lock; manifest final snapshot

## Handoff Chain

- **Continues from**: [2026-08-01-162550-weekly-retro-w31-fixes.md](./2026-08-01-162550-weekly-retro-w31-fixes.md)
  - Previous title: Weekly retro W31 — 三 repo 修繕 merge/push/部署完成；#2 diff 待核准
- **Supersedes**: None

## Current State Summary

W31 skill-fleet 砍伐季全部完成並收尾：lock 由 104 收斂至 70（36 移除、2 新建 ask-nova/writing-artifacts、11 支 mattpocock 上游孤兒 vendor 進本 repo 改源 ohyeh/agent-scripts）。manifest artifacts 由四份精煉為兩份：spinout-work-order（已完結史料）與 skill-grouping-structure（設計文，論點已併入 skill-manifest 三層說明卡）直接刪除，git 歷史即封存。全數 commit + push 至 origin/main，最新 `242803a`。mbp14 與 mac-mini-m2 已收斂，remote2 離線待佈署。

## Codebase Understanding

## Architecture Overview

三層投影：L0 `~/.agents/.skill-lock.json`（安裝事實）→ L1 `artifacts/manifests/skill-manifest.html`（全量花名冊＋installed-vs-adopted gap 儀表板，由 gen-manifest.mjs 程式生成）→ L2 `skills/using-skills/SKILL.md`（僅路由 adopted 面）。deploy 契約：repo `skills-lock.json` canonical，`scripts/deploy.sh` 拉 GitHub main；完全收斂需先 `npx skills remove --all -g`。

## Critical Files

| File | Purpose | Relevance |
|------|---------|-----------|
| artifacts/manifests/skill-manifest.html | L1 花名冊（程式生成） | footer 有 lock sha256 前 8 hex 戳記，不符即 STALE |
| artifacts/manifests/workflow-manifest.html | workflow recipes 分佈快照 | 由 workflow-manifest.workflow.js recipe 再生 |
| skills-lock.json | repo canonical lock（70 支） | deploy 的安裝依據 |
| skills/using-skills/SKILL.md | L2 routing map | 只路由 adopted 面 |
| scratchpad gen-manifest.mjs | skill-manifest 產生器 | 在 session scratchpad，未入 repo（gotcha） |

## Key Patterns Discovered

- 「檔案越少越好」的極限＝每個獨立程式 writer 一份：skill-manifest（gen-manifest.mjs / skill-lock 源）與 workflow-manifest（recipe / workflows 目錄源）資料源與更新節奏不同，不再合併——已向用戶說明並獲 OK。
- footer lock-hash 戳記把「新鮮度」機械可驗證化：`shasum -a 256 ~/.agents/.skill-lock.json` 前 8 hex 對比 footer。
- artifact 整頁重寫易誤刪舊 section（54aeec1 弄丟閉環 SVG）——重生前先 diff 舊版 section 清單。

## Work Completed

## Tasks Finished

- [x] 砍伐季 36 支移除、11 支 vendor、lock 102→70、fleet 部署（mbp14、m2）
- [x] 刪除 spinout-work-order.html（git rm）與 scratchpad skill-grouping-structure.html
- [x] workflow-manifest：W31 補記併入正文；救回 00 L1 單題閉環 SVG；stop-slop 去贅述（收斂事實只講一次、doctrine 細節只留 §04）
- [x] skill-manifest：generator 修正 unmanaged 誤列（只剩 arc-artifact-fetcher）、footer 加生成時間＋lock hash
- [x] 兩份 artifact 以同 URL 重新發佈

## Files Modified

| File | Changes | Rationale |
|------|---------|-----------|
| artifacts/manifests/workflow-manifest.html | 補記併入、SVG 復原、去贅述 | 用戶指出「改版的廢話更多」＋閉環圖消失 |
| artifacts/manifests/skill-manifest.html | 重新生成（hash footer、unmanaged 修正） | 新鮮度可驗證 |
| artifacts/manifests/spinout-work-order.html | 刪除 | 已完結，git 歷史即封存 |

## Decisions Made

| Decision | Options Considered | Rationale |
|----------|-------------------|-----------|
| 刪除而非凍結兩份史料 | frozen banner vs git rm | 用戶裁定：「刪了就移除 有GIT紀錄 最新版不需要」 |
| 兩份 manifest 不再合併為一 | tabs 單頁 vs 互鏈兩頁 | 各有獨立 writer 與資料源；合併需重做樣式＋generator，用戶 OK 停在兩份 |

## Pending Work

## Immediate Next Steps

1. remote2 上線後：`npx skills remove --all -g` ＋ `scripts/deploy.sh` 收斂。
2. 兩份已刪頁面在 claude.ai 的 artifact 頁仍存在（私有）——要清需用戶在 claude.ai/code/artifacts 手動刪。
3. workflow-manifest artifact 有分享連結且觀看者釘在舊版——用戶需在 share 選單推進 shared version。

## Blockers/Open Questions

- [ ] remote2 hostname 解析失敗（離線），無法佈署。

## Deferred Items

- gen-manifest.mjs 只在 session scratchpad——下次改 skill-manifest 前考慮收進 repo（例如 scripts/）。
- `~/.claude/skills` 斷 symlink 清理＋check-rules-invariants.mjs 加斷鏈檢查（backlog）。
- design-roles.md Role 1 仍列未安裝的 frontend-design（既有 drift，未裁決）。
- arc-artifact-fetcher 長期 lock 外（backlog 續留）。

## Context for Resuming Agent

## Important Context

manifest 活文件只剩兩份，各由程式生成——手改 HTML 前先確認是否該改 generator。skill-manifest 的 generator 在 scratchpad（session 專屬目錄，可能已被清），必要時從 git 歷史或重寫；workflow-manifest 由 repo 內 `skills/using-workflows/workflows/workflow-manifest.workflow.js` recipe 再生，但注意 recipe 產出不含後續手工修訂（SVG 復原、去贅述）——重跑 recipe 會蓋掉這些，先比對。

## Assumptions Made

- 兩份 artifact 的 claude.ai URL 固定：skill-manifest `83743cc1-…`、workflow-manifest `6733cfaf-…`；同 session 以同檔路徑 republish 即同 URL。

## Potential Gotchas

- workflow-manifest.workflow.js recipe 重跑會覆寫手工修訂（閉環 SVG 章節、去贅述文字）——需先把這些改動回饋進 recipe 模板，否則再生即回退。
- `npx skills experimental_install` 不會移除多餘 skill、也不重寫機器的 `~/.agents/.skill-lock.json`；完全收斂必須先 remove --all。
- PromptScript「does not support global skill installation」錯誤為 cosmetic，檔案/lock/symlink 實際都到位。

## Environment State

## Tools/Services Used

- npx skills（add/remove/experimental_install）、scripts/deploy.sh、gh/git、Artifact 發佈。

## Active Processes

- 無。

## Environment Variables

- 無相關。

## Related Resources

- skill-manifest artifact: https://claude.ai/code/artifact/83743cc1-4547-42d4-9584-c8591d238339
- workflow-manifest artifact: https://claude.ai/code/artifact/6733cfaf-836a-4ab8-ba9d-89a67c80ac2f
- 前一份 handoff：.claude/handoffs/2026-08-01-162550-weekly-retro-w31-fixes.md
