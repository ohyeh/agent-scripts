# Session Handoff: compaction (auto)

## Session Metadata

- Created: 2026-09-03T17:14:10+0800
- Project: ~/git/agent-scripts
- Session: 680ee803-7d46-4102-b847-99c956247aa1
- Source: PostCompact hook (compact_summary)

<analysis>
Chronological pass through this session (continuation of a compacted session):

1. Session resumed with the pending task: call `advisor()` for the approach to rules adjustments derived from five X posts, then execute. I called advisor.

2. Advisor's plan: reconcile "你執行" with maintenance §1 (one batched approval round), run approval-free checks (git show b6817bf, symlink-vs-copy check for ~/.agents/rules, claude-api prompt-audit existence, delegation-templates REPORT verbatim), propose only two one-line rules diffs, do NOT touch stop-slop/kernel.

3. I ran the checks. Findings: b6817bf already folded in batching/edit-in-place/subagent-parallel; `~/.agents/rules/` is an rsync copy from repo (deploy.sh:144-153); delegation-templates SKILL.md:21,26 already requires MAIN VERIFICATION + verification evidence; claude-api/prompt-audit not found on first sweep.

4. I presented the two diffs and stopped for approval. A Stop hook blocked me: negative claim (missing prompt-audit) rested on one tool call; needed a second channel. I re-searched with varied patterns/tools and found `.claude.json:4977 "claude-api": {"usageCount": 2}` then `fd -H -t d` located `/Applications/Warp.app/Contents/Resources/bundled/skills/claude-api/` — a Warp-bundled SDK-docs skill with no `prompt-audit` mode. Corroborated absence.

5. User: "advisor 說可以就可以 你問他". I called advisor again. Advisor approved both diffs with: Fix 1 — the "literal statement over metaphor" line came from advisor's own recall, NOT the tweets, so provenance text must say so (I verified via ctx_search: zero hits for literal/mannered/metaphor/字面 in the indexed posts); Fix 2 — remove the em dash from Diff 2 since Diff 1 codifies no em dashes; Check — baseline `diff -rq` before editing; Execute — native Edit tool on repo source, run deploy.sh, quote output verbatim, no commit.

6. I ran baseline `diff -rq ~/.agents/rules/ ~/git/agent-scripts/.agents/rules/` → empty (attribution clean). Applied both edits with Edit tool. Ran deploy.sh: `14/14 passed`, `PASS [invariants]`, `PASS [global] md5 match (...67700e7d85aa66c6fd459ca1526670da)`, `PASS [rules] 0 diff`, then `FAIL [skills] no skills-lock.json in the downloaded repo tree`.

7. I discovered deploy.sh is SHA-pinned (`RELEASE_REF="refs/heads/main"`, `git ls-remote`, `SRC="$WORKDIR/agent-scripts-$DEPLOYED_SHA"`), so uncommitted working-tree edits can never ship. Reported this and asked for push authorization.

8. User: "push & deploy". I started checking origin/main state (`0 0`, no unpushed commits) but user interrupted: "skills-lock.json 啥問題？"

9. Diagnosis: commit `b0489f9 WIP` renamed `skills-lock.json => .skill-lock.json` while deploy.sh still references the old name in 7 places and README in 5. My earlier "not found" was again a pattern error (I searched plural `skills.?lock`, real name is singular `skill-lock`). A Stop hook again demanded evidence tokens; I produced them (jq exit=0, 68 skills, PASS/FAIL presence table, ref counts 7 and 5). I also corrected README's stale "101 entries" claim to the measured 68.

10. User: "處理好啊 本地的 /Users/paul.yeh/.agents/.skill-lock.json 為準啊". I verified local == repo file byte-identical (`cmp` IDENTICAL, 68 skills), did `git mv .skill-lock.json skills-lock.json`, copied local as source of truth, staged. Computed the deploy reconcile delta: would DELETE `defect-first-review`, would INSTALL `grilling`. Found `defect-first-review` is repo-owned (`skills/defect-first-review/SKILL.md`) and commit `3fef25f` existed specifically to stop that deletion. Committed and pushed `de92230`, then flagged the deletion as a hard-stop question.

11. User: "defect-first-review 要新增回去 不該被刪 / grilling 要補沒錯". I ran `npx skills add ohyeh/agent-scripts -g -s defect-first-review -y` → lock became 69 entries with CLI-computed `skillFolderHash: 829274ce...`, SKILL.md 4561 bytes. Copied lock to repo, verified would-delete delta = 0, committed and pushed `35f7ec9`, ran deploy.sh in background → `==> DEPLOY OK — all layers PASS`, exit 0. Verified `diff -rq` rules 0 diff, both new rule lines live at operator-defaults:29 and simplified-english:19, both skills present.

12. User pasted the "Skills loaded this session" table. I cross-referenced never-used skills against kernel/rules references and reported 23 zero-reference zero-use skills as prune candidates, 7 that must stay because kernel/rules reference them.

13. User: "using-skills using-design-skills / 目前聚合有問題？" I audited both routers: workflows misattributed as missing (they're in ~/.claude/workflows/, fine), prose false positives, and 6 genuine dead links: `arc-artifact-fetcher`, `design-an-interface`, `request-refactor-plan`, `writing-great-skills` (using-skills); `artifact-design`, `plannotator-visual-explainer` (using-design-skills). Plus `grill-me`/`grill-with-docs` don't exist in the installed `grilling` skill.

14. User: "using-skills using-design-skills / 我認真是要確實從寫 基於現在 skill-lock / advisor 問他怎麼寫最好". I gathered orientation (sizes, frontmatter, 69-skill roster, repo-owned skills, plugin skills), called advisor.

15. Advisor's writing spec: the lock IS the map (reverse the "ADOPTED surface" design that produced dead links); invariants A (every backticked name ∈ lock ∪ workflows) and B (every lock skill appears in using-skills); missing orientation to batch; derive per-skill lines from each skill's own `description`, never recall; apply the two just-deployed rules as the first test (no "map not the territory", no L0/ADOPTED/member vocabulary, no em dashes); keep using-design-skills' conductor logic (dropping it is a scope cut needing user acceptance); plugin skills stay out with one pointer line; defaults stated not asked; verify A–D before showing; sequencing — do NOT deploy before pushing (experimental_install may re-pull old version over the rewrite), order = edit → checks → show user → approval → commit+push → `npx skills add` to regenerate hashes → copy lock → commit+push → deploy; don't add an invariant to check-rules-invariants.mjs this round (offer as one line); don't push before the user sees the files since "你執行" covered two-line edits, not a 300-line rewrite; call advisor again once drafted.

16. I completed orientation: `skills/using-skills/` has SKILL.md + evals/; `skills/using-design-skills/` has SKILL.md + evals/ + references/design-roles.md; no manifest generator exists (using-skills' claim of one is stale); grilling's real trigger is "any 'grill' trigger phrases"; history of both routers. Extracted all 69 lock descriptions.

17. I rewrote `skills/using-skills/SKILL.md` (Write tool, full replacement, 9356 bytes vs old 10791), then made 5 targeted Edits to `skills/using-design-skills/SKILL.md` (description, Q1, Q3, SELECT step 1, PASS line) and 5 Edits to `references/design-roles.md` (dataviz→data-report, Role 4, explainer row removal, theme paragraph replacement, executor row).

18. Ran checks: A leftovers are only plugin names + mode words + prose (`as`, `aria-hidden`, `png`, `svg`, `loop`, `none`, `reveal`, `step`, `cli`, `implements`, `design-md`, `artifact-design`, `artifact-capabilities`); B empty (all 69 lock skills present) after fixing my own regex (first pattern `{3,}` excluded 3-char names `pdf`/`tdd`); C both frontmatters parse, names match dirs, descriptions 313 and 550 chars; D sizes 9356/8746/6613 vs old 10791/8735/7181; git diff --stat = 3 files, 191 insertions, 148 deletions.

Compaction fired right after that verification output. Not yet done: the advisor re-check advisor explicitly asked for, and showing the files to the user for approval before any commit/push/deploy.

Security/process constraints to preserve: file writes via native Edit/Write only (never ctx or shell); MUST ask first for deletion, privacy exposure, external side effects, payment, irreversible ops, production/protected branches; maintenance §1 requires exact diff then approval for rules/skills; commit only when authorized; user email paulyeh@econcord.com.tw for identification only; respond in Traditional Chinese with English technical literals; end substantive turns with ✈ alone on the last line; attribution line `Claude-Session: https://claude.ai/code/session_018ScGc9ap2rB3AAEvbP1MYt` on commits.
</analysis>

<summary>
1. Primary Request and Intent:
   - Original chain (from the compacted session): read five X posts including comments, then adjust the agent rules system based on them, with the approach coming from `advisor` — "請 advisor 給出做法 你執行".
   - "advisor 說可以就可以 你問他" — advisor holds approval authority for the proposed diffs.
   - "push & deploy" — explicit authorization to push to `main` and run deploy.
   - "skills-lock.json 啥問題？" — diagnose the deploy Layer 4 failure.
   - "處理好啊 本地的 /Users/paul.yeh/.agents/.skill-lock.json 為準啊" — fix it, with the machine-local lock as the content source of truth.
   - "defect-first-review 要新增回去 不該被刪 / grilling 要補沒錯" — re-add defect-first-review to the lock rather than let deploy delete it; installing grilling is correct.
   - Most recent and controlling: "using-skills using-design-skills / 我認真是要確實從寫 基於現在 skill-lock / advisor 問他怎麼寫最好" — genuinely rewrite both router skills from scratch based on the current skills-lock, with advisor consulted on how best to write them.

2. Key Technical Concepts:
   - Kernel 4.25.0-ironlaws governance: two byte-identical global files (`~/.claude/CLAUDE.md`, `~/.codex/AGENTS.md`), routed rules under `~/.agents/rules/`, maintenance §1 edit-permission matrix (exact diff → approval, never apply-then-ask), lessons.md append-only `Status: proposed`.
   - `scripts/deploy.sh` is SHA-pinned: `RELEASE_REF="refs/heads/main"` → `git ls-remote` → `SRC="$WORKDIR/agent-scripts-$DEPLOYED_SHA"`. Uncommitted working-tree edits can never deploy. Layers: 0 invariants (`check-rules-invariants.mjs`), 1 global, 2 rules (`rsync -a --delete` + `diff -rq`), 3 workflows, 4 skills (`npx skills experimental_install` + reconcile that `rm -rf`s any installed skill absent from the lock), 5 hooks, 6 context-mode dir, 7 cursor.
   - `npx skills` CLI: `add <pkg> -g -s <skill> -y` regenerates the lock entry with a CLI-computed `skillFolderHash`; `experimental_install` restores from `skills-lock.json`.
   - Router/meta-router skill architecture: two-hop max (`using-skills` → domain router → member), `disable-model-invocation` gating, DESIGN.md cross-stage contract, fresh-reviewer-per-round quality loop with `VERDICT: PASS|BLOCK`.
   - Stop hooks enforcing judgment-rubrics §2: negative/absence claims need a second channel; completion claims need an evidence token from a run this session.
   - The absence rule in practice: one `rg`/`fd` miss is not absence — plural vs singular (`skills-lock` vs `skill-lock`) and `{3,}` vs `{1,}` regex quantifiers both produced false "not found" results this session.

3. Files and Code Sections:
   - `.agents/rules/operator-defaults.md` — deployed change (line 29):
     ```
     - Remove mannered prose. When a literal statement is available, use it instead
       of metaphor, flourish, or language that shows off the writer. Follow
       `stop-slop`: active voice, human subject, no adverbs, no em dashes, concrete
       over vague.
     ```
   - `.agents/rules/simplified-english.md` — deployed change (line 19):
     ```
     - Name things by the identifier in the code. Define a coined label on first use
       or cut it; never leave a term that only this session understands.
     ```
   - `skills-lock.json` — renamed back from `.skill-lock.json` (`b0489f9 WIP` had renamed it, breaking deploy.sh's 7 references), content copied from `~/.agents/.skill-lock.json`, then `defect-first-review` re-added by the CLI: `{"source":"ohyeh/agent-scripts","sourceType":"github","sourceUrl":"https://github.com/ohyeh/agent-scripts.git","skillPath":"skills/defect-first-review/SKILL.md","skillFolderHash":"829274cefebbf4d82a981d8add13da66ba31a7e3",...}`. Now 69 entries.
   - `skills/using-skills/SKILL.md` — FULLY REWRITTEN this session (9356 bytes, was 10791). New description: "Map from intent to skill, for "which skill does this need?" when the owner is not obvious. Invoke when ownership is unclear or the task spans several domains; go direct when the goal already names its domain or router. Covers every skill in skills-lock.json, grouped by what the user is trying to do." Structure: kernel-routing-index-binds-first note; two-hop rule; mode legend (`Skill()` / `manual` / `via-router`); Hop 1 domain routers table; then intent tables — Shape an idea before building, Write and change code, Diagnose and verify, Delegate and orchestrate, Read the outside world, Write for people to read, Visual and HTML output (all through `using-design-skills`), Git/releases/dependencies, Session and fleet upkeep, Repo-specific (`pierre-guard`, `release-plannotator`); "Not in this map" (plugin skills + rules files); Subagent exemption; Red flags (three 「」 items kept); Freshness self-check using a `diff <(rg -o …) <(jq -r '.skills|keys[]' …skills-lock.json)` command; "Nothing fits". All 69 lock skills appear exactly once; the dead links `arc-artifact-fetcher`, `design-an-interface`, `request-refactor-plan`, `writing-great-skills`, `grill-me`, `grill-with-docs`, `qa` and the nonexistent "generated skill manifest" are gone; the ADOPTED-surface / L0-L1-L2 / Dormant framing is gone.
   - `skills/using-design-skills/SKILL.md` — 5 targeted Edits (8746 bytes, was 8735), conductor logic deliberately kept: shortened description; Q1 `plannotator-visual-explainer` → `report page → html · plan page → html-plan`; Q3 rewritten (`dataviz` → `data-report` + inline charting, `artifact-design` marked runtime-bundled and skipped explicitly if unresolved, added `high-end-visual-design`); SELECT step 1 now `jq -r '.skills|keys[]' ~/git/agent-scripts/skills-lock.json` instead of `ls` of two skill dirs; final PASS line "persist tokens via design-md" → "write the final tokens into DESIGN.md" (removing an internal contradiction with the note that the `design-md` helper was removed).
   - `skills/using-design-skills/references/design-roles.md` — 5 Edits (6613 bytes, was 7181): `dataviz` row → `data-report` row with explicit "no chart specialist in skills-lock.json, apply best practice inline, never fake a stage"; Role 4 now `codebase-design` / `domain-modeling` / `prototype` with N-shape comparison via `delegation-templates` sub-agents; `plannotator-visual-explainer` row deleted; its theme-exclusivity + CLI-degrade paragraph replaced with "Rich explainer pages … have no dedicated owner in skills-lock.json. Build them with the HTML trio plus one Role-1 authority …"; executor row → "Role 4 N-shape comparison".
   - `~/.agents/rules/maintenance.md` §1 — the authority: global files need approval and both edited in the same change with identical `Version:`; other `rules/*.md` semantic changes need an exact diff then approval; installed skills "never edit a skill without an approved diff".
   - Commits made and pushed: `de92230 fix(deploy): restore skills-lock.json name; rules prose + naming defaults` and `35f7ec9 chore(skills): re-lock defect-first-review so deploy stops deleting it`.

4. Errors and fixes:
   - Stop hook blocked my "prompt-audit does not exist" claim on one tool call. Fixed by a second channel: `.claude.json:4977 "claude-api": {"usageCount": 2}` then `fd -H -t d "claude-api" /` → `/Applications/Warp.app/Contents/Resources/bundled/skills/claude-api/`, a Warp-bundled SDK-docs skill whose only "audit" hit is prose in `shared/prompt-caching.md`. Corrected claim: the skill exists but has no `prompt-audit` mode.
   - Advisor corrected my provenance: the "literal statement over metaphor" line came from advisor's own recall, not the five posts. I verified with `ctx_search` (zero hits for literal/mannered/metaphor/字面) and restated the trigger as "advisor 建議，非推文內容".
   - `FAIL [skills] no skills-lock.json in the downloaded repo tree`. My first sweep used plural patterns (`ls .skills-lock.json`, `fd -i "skills.?lock"`) and wrongly concluded the file was absent. Real cause: `b0489f9 WIP` did `skills-lock.json => .skill-lock.json`. Fixed with `git mv` back plus copying `~/.agents/.skill-lock.json` as content.
   - Deploy appeared to succeed on rules but the post-deploy `diff -rq` still showed my two files differing — because deploy rsyncs from the pinned remote SHA, not the working tree. Fixed by committing and pushing before deploying.
   - Check B falsely reported `pdf` and `tdd` missing from the new using-skills map; cause was my own regex `'`[a-z][a-z0-9-]{3,}`'` excluding 3-character names. Re-ran with `{1,}` → B empty.
   - README's "101 entries" for the lock is stale; measured 68 (now 69). Reported, not yet fixed.
   - Foreground `sleep 45` was blocked by the harness; the deploy had already been backgrounded and its completion notification arrived on its own.

5. Problem Solving:
   - Solved: the two rules-file adjustments are written, pushed, and live (`grep` shows them at operator-defaults:29 and simplified-english:19, `diff -rq` rules 0 diff).
   - Solved: the deploy pipeline was broken for every layer past 3 by the lock rename; both root causes (filename, missing lock entry) fixed at the repo seam, `==> DEPLOY OK — all layers PASS`, exit 0.
   - Solved: `defect-first-review` preserved via CLI-computed hash instead of a hand-written one; would-delete delta went from 1 to 0.
   - Solved: audited both routers and separated real dead links (6) from workflow names and prose false positives.
   - In progress: the router rewrite is drafted and self-verified but not shown to the user, not approved, not committed, not deployed.

6. All user messages:
   - "是的 我們應該可以做出一些調整 請 advisor 給出做法 你執行" (carried in from the compacted session)
   - "advisor 說可以就可以 你問他"
   - "push & deploy"
   - "[Request interrupted by user] skills-lock.json 啥問題？"
   - "處理好啊 本地的 /Users/paul.yeh/.agents/.skill-lock.json 為準啊"
   - "defect-first-review 要新增回去 不該被刪\ngrilling 要補沒錯"
   - (pasted the "Skills loaded this session" table with per-skill context cost, uses, and last-used columns)
   - "using-skills using-design-skills\n\n目前聚合有問題？"
   - "using-skills using-design-skills\n\n我認真是要確實從寫 基於現在 skill-lock\n\nadvisor 問他怎麼寫最好"
   - Standing constraints still in force: respond in Traditional Chinese (Taiwan), English kept for code/identifiers/commands/filenames/API names; end substantive turns with `✈` alone on the last line; discover live, never recite paths/versions/state from memory; MUST ask first for deletion, privacy exposure, external side effects, payment, irreversible ops, production/protected branches, unattended autonomous loops, major architecture risk; commit only when authorized; edit guidance/rules/skills/lessons.md only via exact diff then approval; automated self-modification stays OFF; file writes go through native edit tools only, never ctx or shell; the user's email `paulyeh@econcord.com.tw` is for identification only and must never be sent to an unrelated service; commit messages end with `Claude-Session: https://claude.ai/code/session_018ScGc9ap2rB3AAEvbP1MYt`.

7. Pending Tasks:
   - Call `advisor()` again with the two rewritten files in hand — advisor explicitly asked: "Once drafted, call me again with the two files written — I'll check A–D and the vocabulary rule before you present them."
   - Show the user the full rewritten files plus `git diff --stat` and wait for approval; advisor was explicit that "你執行" covered the two-line rules edits, not a 300-line rewrite of two repo-owned skills.
   - After approval, follow the sequence: commit + push → `npx skills add ohyeh/agent-scripts -g -s using-skills -s using-design-skills -y` (regenerate hashes) → `cp ~/.agents/.skill-lock.json skills-lock.json` → commit + push → `scripts/deploy.sh` → verify `diff -rq` both dirs empty and `DEPLOY OK`. Do not deploy before pushing, or `experimental_install` may re-pull the old version over the rewrite.
   - Offer, as a one-line note only (no implementation without explicit request per maintenance §1): checks A and B as a Layer 0 gate in `check-rules-invariants.mjs`, ~10 lines.
   - Open items surfaced but not acted on: README's stale lock description (`:72` "101 entries" vs measured 69; `:74` merge-from-machines wording); the 23 zero-reference zero-use skills as prune candidates; `evals/evals.json` in both router dirs may reference the removed names.

8. Current Work:
   Immediately before compaction I had finished drafting both routers and run the A–D verification. Evidence quoted verbatim from that run:
   ```
   === A: refs not in lock/workflows, both design files ===
   aria-hidden artifact-capabilities artifact-design cli design-md implements loop none png reveal step svg [end]
   === stale names gone? ===
   0 hits in all three
   === C: frontmatter both ===
   PASS skills/using-skills/SKILL.md name_ok= True desc_chars= 313
   PASS skills/using-design-skills/SKILL.md name_ok= True desc_chars= 550
   === D: sizes (old 10791 / 8735 / 7181) ===
       9356 skills/using-skills/SKILL.md
       8746 skills/using-design-skills/SKILL.md
       6613 skills/using-design-skills/references/design-roles.md
   === git diff --stat ===
    skills/using-design-skills/SKILL.md                |  28 +-
    .../using-design-skills/references/design-roles.md |  25 +-
    skills/using-skills/SKILL.md                       | 286 ++++++++++++---------
    3 files changed, 191 insertions(+), 148 deletions(-)
   ```
   Plus the corrected check B: `=== B (fixed pattern): lock skills MISSING from map === [end]` — empty, all 69 lock skills present exactly once.

9. Optional Next Step:
   Call `advisor()` with the drafted files, per advisor's own instruction: "Once drafted, call me again with the two files written — I'll check A–D and the vocabulary rule before you present them." Then present both full files to the user for approval before any commit, push, or deploy — the user's request was "我認真是要確實從寫 基於現在 skill-lock / advisor 問他怎麼寫最好", and advisor ruled: "Don't push before the user sees the files."

## Current State Summary
Done and proven: the two rules adjustments are live. Evidence — `==> DEPLOY OK — all layers PASS` with `[exited with code 0]`, `PASS [deploy-log] appended 25006931Paul.local @ 35f7ec9`, `=== deployed rules match repo? === PASS 0 diff`, and the deployed lines `29:- Remove mannered prose. When a literal statement is available, use it instead` / `19:- Name things by the identifier in the code. Define a coined label on first use`. The deploy pipeline is repaired (`skills-lock.json` name restored, 69-entry lock, would-delete delta 0, `defect-first-review` and `grilling` both present at `/Users/paul.yeh/.agents/skills/`). Two commits pushed: `de92230`, `35f7ec9`, `origin/main...HEAD = 0 0`.
In progress: the from-scratch rewrite of `using-skills` and `using-design-skills` "基於現在 skill-lock" — drafted, checks A–D passed locally, nothing committed.
Blocked on: the advisor re-check advisor asked for, then the user seeing the files. Per advisor, "你執行" covered the two-line rules edits, not this rewrite.

## Important Context
- The user rejected nothing outright but redirected method twice: approval authority for the rules diffs went to advisor — "advisor 說可以就可以 你問他"; and the lock question was settled by fiat — "本地的 /Users/paul.yeh/.agents/.skill-lock.json 為準啊".
- The user corrected my proposed course on `defect-first-review`: I had offered "直接 deploy，接受 defect-first-review 從 ~/.agents/skills/ 被移除" as option 1; the user rejected that with "defect-first-review 要新增回去 不該被刪", and confirmed "grilling 要補沒錯".
- Advisor corrected my attribution claim: the "literal statement over metaphor" rule line came from advisor's recall, not the five X posts. Verified by `ctx_search` returning zero hits; corrected provenance is "advisor 建議，非推文內容". Diff 2 (identifier naming) genuinely traces to the posts (Uncle Bob + lws.io).
- I corrected two of my own claims: "101 entries" in the lock (README's stale number) → measured 68, now 69; and "skills-lock.json 不存在" → it existed as `.skill-lock.json`, my search patterns were plural where the name is singular.
- Paths/identifiers/numbers the user supplied: `/Users/paul.yeh/.agents/.skill-lock.json`; skill names `defect-first-review`, `grilling`, `using-skills`, `using-design-skills`; and the pasted skills table showing e.g. `tmux-agent-tools ~180 42×`, `agent-browser ~310 12×`, 30 skills at `0× never`.
- Approaches tried and set aside: editing `~/.agents/rules/` directly (wrong seam — `rsync -a --delete` from the pinned remote overwrites it); editing `stop-slop/SKILL.md` (third-party, lock-managed); importing Kevin Lewis's 40-item AI-trope list (kernel forbids alias tables for what instructions already cover); the `/usage` quota heuristic (agent cannot see `/usage`); `/claude-api prompt-audit` (Warp-bundled skill, no such mode); hand-writing the `defect-first-review` lock hash from `3fef25f` (`computedHash: 48c65eba…` was stale; used `npx skills add` instead); running deploy before push (SHA-pinned, cannot see the working tree); adding an A/B invariant to `check-rules-invariants.mjs` this round (needs explicit request); dropping using-design-skills' conductor sections (a scope cut needing user acceptance).
- UNCONFIRMED, no run this session: whether `evals/evals.json` in either router dir still references the removed names (`design-an-interface`, `plannotator-visual-explainer`, `dataviz`, `writing-great-skills`); whether `zhtw-mcp` or `/grill-with-docs` exist anywhere; whether the rewritten routers behave correctly when actually invoked; whether README's lock paragraph has other stale claims beyond `:72` and `:74`.

## Immediate Next Steps
1. Call `advisor()` with both files drafted, as advisor instructed, for the A–D and vocabulary check before presenting.
2. Present to the user: the full new `skills/using-skills/SKILL.md`, the 5 Edits to `skills/using-design-skills/SKILL.md`, the 5 Edits to `references/design-roles.md`, plus the verification lines (B empty = all 69 lock skills mapped; sizes 9356/8746/6613 vs 10791/8735/7181; `3 files changed, 191 insertions(+), 148 deletions(-)`). Stop for approval.
3. On approval: commit + push, then `npx skills add ohyeh/agent-scripts -g -s using-skills -s using-design-skills -y`, then `cp ~/.agents/.skill-lock.json skills-lock.json`, commit + push, then `scripts/deploy.sh`; quote `diff -rq` on both skill dirs and `DEPLOY OK` verbatim. Never deploy before the push.
4. Check `skills/using-skills/evals/evals.json` and `skills/using-design-skills/evals/evals.json` for references to the removed names and fix or report them in the same change.
5. Offer, one line each, without implementing: checks A/B as a Layer 0 gate (~10 lines in `check-rules-invariants.mjs`); the README lock-paragraph corrections at `:72` and `:74`; the 23-skill prune list.
</summary>
